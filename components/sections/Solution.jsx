const Solution = () => (
  <section className="sec solution">
    <div className="container container--narrow reveal">
      <div className="eyebrow">The shift</div>
      <h2 className="display-lg" style={{ marginTop: 12 }}>
        Not another tool to add.<br /><em style={{ color: 'var(--peach-200)', fontStyle: 'normal', fontWeight: 800 }}>The system</em> underneath them all.
      </h2>
      <p className="lede" style={{ marginTop: 20, color: 'rgba(255,255,255,0.7)' }}>
        Data, pipelines, workflows, and AI — designed together as one fabric, not separate products glued by APIs.
      </p>
    </div>
  </section>
);

export default Solution;
